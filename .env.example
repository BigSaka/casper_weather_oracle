# Copy to .env and fill in. Never commit the real .env or the agent's
# secret key file.

WEATHER_API_KEY=
WEATHER_API_KEY_BACKUP=

CASPER_NODE_URL=https://node.testnet.casper.network/rpc
CASPER_CHAIN_NAME=casper-test

# Filled in after `cargo odra deploy` against testnet
ORACLE_CONTRACT_HASH=
REPUTATION_CONTRACT_HASH=

AGENT_SECRET_KEY_PATH=./keys/agent_secret_key.pem
